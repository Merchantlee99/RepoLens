# Optional GitHub token. Recommended for public deployments to avoid tokenless rate limits.
GITHUB_TOKEN=your_github_personal_access_token

# Public site URL used for absolute metadata links.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Optional local debug logging.
# REPOLENS_DEBUG=1
